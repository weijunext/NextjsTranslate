import { Homepage } from '@/components/home/Homepage';
import { getStargazersCount } from '@/lib/getStargazersCount';

export async function getStaticProps() {
  const stars = await getStargazersCount();
  return {
    props: {
      ssg: {
        stars,
      },
    },
  };
}

<Homepage />